services:
  - type: web
    name: wiki-react-redux
    runtime: node
    plan: free
    region: oregon
    branch: master
    buildCommand: yarn install && yarn build
    startCommand: node server.js
    healthCheckPath: /api
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
